.list-group.mt-5.mr-5.category_container
        if categories.length > 0
                each category in categories
                        a(href=`/categories/${category._id}` class=category._id==selectedCategory?"list-group-item active":"list-group-item").link #{category.name} 